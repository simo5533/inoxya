---
description: -- No redesign, only polish. - Keep luxury theme (black/gold/ivory). - Small incremental changes only. - Every SEO change must be Next.js Metadata API compliant. - No broken links/images; always add alt text. - Keep footer contact values exactly as provided.  Do minimal, reversible changes. No big refactor. - Never delete/overwrite the existing SQLite DB file in /data. - Always keep the current routes and UI behavior. - Fix by adding a DB adapter for better-sqlite3/sql.js instead of hacking callers. - Prevent build-time DB reads: force-dynamic + runtime nodejs for DB pages. - Add logs only where needed; never log secrets or personal data. - After changes, always run: smoke tests + dev run + build. - If a command fails, fix it before continuing. - Commit per phase with clear messages.Ne jamais supprimer/renommer des dossiers, pages, routes, ou tables DB sans nécessité.  Ne jamais réinitialiser/seed la DB ni toucher aux données existantes.  Changements minimaux, tests après chaque étape.  Toujours expliquer: cause → fix → test.  Pas de refactor massif. Pas de “rewrite whole file” si on peut patcher.  Toujours garder la compatibilité Windows.  Si un module natif casse (better-sqlite3), stabiliser avec fallback (sql.js) d’abord.
alwaysApply: false
---
